import React from 'react';
import TapeSection from "@/components/shared/Tape Section/TapeSection";

function SectionsTape() {
    return (
        <div className={`flex`}>
            <TapeSection svg={} altName={} text={}/>
        </div>
    );
}

export default SectionsTape;